#include "../include/fractol.h"

/*
	z n+1 = z n 2 + c
	z n est un nombre complexe, initialisé à z 0 = 0.
	c est un nombre complexe représentant le point de départ sur le plan complexe.
	n est le nombre d'itérations.
*/
void	draw(t_data *data)
{
	ft_printf("draw\n");
	(void)data;
}
